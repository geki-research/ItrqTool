using Xunit;

namespace ItrqTool.Tasks.Tests;

// Placeholder — add one test class per task implementation following the checklist
// in CLAUDE.md: success path, failure path, cancellation propagation.
public sealed class TasksPlaceholderTests
{
    [Fact(Skip = "Remove when first task is implemented")]
    public void Placeholder() { }
}
